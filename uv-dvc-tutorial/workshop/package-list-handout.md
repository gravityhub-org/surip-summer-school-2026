# Package list (reference — students use SETUP.md)

Same list as [SETUP.md](../SETUP.md). Students run `uv init`, then `uv add` for each package.

```
numpy
scipy
astropy
matplotlib>=3.7,<3.11
healpy
colossus
lenstronomy
requests
scienceplots
h5py
pesummary
dvc
autolens
```

**Note:** `scienceplots` requires `matplotlib<3.11` — pin as shown above.
